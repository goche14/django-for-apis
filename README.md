# django-for-apis

They take your Django models (Python objects) and turn them into JSON so your frontend or API consumers can understand them. And they can do the reverse too — take JSON from a request and turn it into a Django model instance
